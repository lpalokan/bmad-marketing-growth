# See PMM orchestrator's positioning brief template for pillars
# (capability MP within the same template). Use that template; this
# stub exists only to satisfy the workflow.yaml brief_template
# reference.
